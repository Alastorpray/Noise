// Feature flags — flip to re-enable a section across nav, routing and SEO.
// Keep in sync with BLOG_ENABLED in functions/sitemap.xml.js and
// functions/_middleware.js (Cloudflare Functions bundle separately).
export const BLOG_ENABLED = false
